import { Box } from "@mui/material";
import { RTMTextReader } from "../../components/RTMTextReader/RTMTextReader";

const ReadingPage = () => {
  return (
      <RTMTextReader></RTMTextReader>
  );
};

export default ReadingPage;
